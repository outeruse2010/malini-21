import { makeStyles } from '@material-ui/core/styles';

export const AppStyles = makeStyles((theme) => ({ 
    title_row: { marginBottom: theme.spacing(1) },
    data_grid_header: {fontSize: 14, color: '512da8'},
 }));